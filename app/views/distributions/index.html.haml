%p#notice= notice
%h1 Listing Distributions
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @distributions.each do |distribution|
      %tr
        %td= link_to 'Show', distribution
        %td= link_to 'Edit', edit_distribution_path(distribution)
        %td= link_to 'Destroy', distribution, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Distribution', new_distribution_path
